# termux-dns-rotator
Automatic DNS rotating daemon for Termux. Cycles public DNS servers (Cloudflare, Quad9, AdGuard, etc.) by rewriting $PREFIX/etc/resolv.conf. Includes termux-services support for boot persistence. No root required.
